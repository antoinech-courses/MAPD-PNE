package org.pneditor.petrinet.models.cheucleclaeys;

/**
 * A package to handle petri network calculations and to save the model we have implemented.
 * It is composed of all the classes that are used for the petri network (4 types of edges, place, transition, petriNetwork).
 * And it also has one abstract class Edge, an interface PetriNetwork and an enum EdgeType to help implementation of the code.
 **/